# Tensor4all.jl Tutorial Notebooks Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Jupyter-first tutorial repository for learning QTTs with `Tensor4all.jl`, using `01_first_qtt_function_and_grid.ipynb` as the reference implementation for the style and structure of the remaining notebooks.

**Architecture:** The repository root contains the notebooks so learners can find them immediately. A root Julia environment makes setup reproducible. Supporting planning notes, optional plots, and future feature-gap notes live under `docs/`, `plots/`, and `data/`.

**Tech Stack:** Julia, Tensor4all.jl, IJulia, CairoMakie, Jupyter notebooks (`.ipynb`), Markdown documentation.

---

## Execution Rule

`01_first_qtt_function_and_grid.ipynb` now exists and has already established
the current standard for teaching style, code exposure, plotting style,
parameter presentation, print style, and overall notebook layout.

From this point on:

- do not redesign the notebook style from scratch for each new tutorial,
- do not change the plotting language unless there is a clear topic-specific
  reason,
- do not introduce a second competing notebook style,
- use Notebook 01 as the default visual and pedagogical reference.

The Rust tutorials in `../rust-Tensor4all` remain the content source, but
Notebook 01 is now the primary style source.

## File Structure

Create or modify these files:

- Modify: `README.md`
  - Learner-facing setup instructions.
  - Notebook order.
  - Local contributor override instructions.
- Create: `Project.toml`
  - Julia environment for the notebooks.
- Create: `Manifest.toml`
  - Generated by Julia `Pkg.instantiate()` after adding dependencies.
- Create: `docs/library-gap-log.md`
  - Local, reviewed list of missing Tensor4all.jl features encountered while writing notebooks.
- Keep: `docs/tutorial-learning-path.md`
  - Existing design document.
- Keep: `01_first_qtt_function_and_grid.ipynb`
  - Existing reference notebook for style and structure.
- Create later, after review:
  - `02_accuracy_bonddims_and_sweeps.ipynb`
  - `03_multivariate_qtts_and_layouts.ipynb`
  - `04_operations_on_qtts.ipynb`
  - `05_fourier_transforms.ipynb`
  - `06_affine_transformations.ipynb`
- Optional later:
  - `plots/`
  - `data/`

## Global Notebook Rules

Use these rules for every notebook:

- Write prose in English.
- Use CairoMakie for all plots.
- Keep pedagogically important code visible in notebook cells.
- Do not hide target functions, QTT construction, evaluation, or plotting in helper files.
- Small helper functions inside a notebook are allowed for repeated plotting or compact error metrics.
- Use compact output: errors, bond dimensions, dimensions, and representative values.
- Do not print full tensors or large arrays.
- Label every plot with meaningful axes and legends.
- Do not mention missing library features inside teaching notebooks.
- If a missing feature blocks progress, record it in `docs/library-gap-log.md`.
- Gap-log entries must be understandable without reading the notebooks. A short
  note about where the issue surfaced is helpful, but the main description
  should stand alone as a package-level record.
- Use the highest sensible Tensor4all.jl API available.
- Do not invent ad hoc notebook-local implementations for missing Tensor4all.jl functionality.

## Reference Notebook Style Specification

All future notebooks should follow the style established by
`01_first_qtt_function_and_grid.ipynb`.

### 1. Overall teaching shape

Each notebook should read like a guided worksheet, not like package
documentation and not like a research draft.

Preferred sequence:

1. title
2. learning goals
3. setup reminder if needed
4. concept section
5. runnable example
6. immediate interpretation
7. deeper explanation after the relevant output is visible
8. one experiment or controlled variation
9. `What to notice`
10. `API recap`

### 2. Section layout and collapsibility

Large headings must be placed in their own Markdown cells.

This applies to:

- `#` notebook titles,
- all `##` headings,
- any `####` or similar subsection heading that should be collapsible on its
  own.

The explanatory text for that heading goes in the following Markdown cell.

This rule is mandatory. It keeps sections cleanly collapsible in notebook UIs.

### 3. Tone

The tone should be calm, precise, learner-facing, and technically serious.

The notebooks should:

- explain what is happening and why it matters,
- avoid sounding like a test script,
- avoid sounding like an internal engineering note,
- avoid unexplained jargon,
- avoid phrases like "obviously", "clearly", or "trivially".

Preferred tone patterns:

- "Here we use ..."
- "This lets us ..."
- "Before looking at one specific grid point, ..."
- "We reuse the same interpolation settings here, so ..."

### 4. Explanation rhythm

Within each section, follow this rhythm:

1. introduce the next idea briefly,
2. run code,
3. inspect output or plot,
4. explain why the result makes sense,
5. invite a small variation when appropriate.

Do not front-load too much theory.

If a concept becomes intuitive only after the student sees the result, delay
the full explanation until after that result. Notebook 01 already models this
well with the `cosh(x)` compactness explanation appearing after the
bond-dimension plot.

### 5. Redundancy control

Avoid explaining the same idea twice at the same level of detail.

Allowed:

- a short preview early,
- a fuller explanation later when the student has the relevant output in front
  of them.

Not allowed:

- duplicated motivation paragraphs,
- repeated API explanations that add nothing new,
- repeated discussion of the same figure.

### 6. Code visibility

Pedagogically important code must remain visible.

Keep visible:

- target functions,
- intervals,
- `R`,
- interpolation parameters,
- QTT construction calls,
- evaluation calls,
- error calculations,
- plot code.

Do not move any of the above into helper files.

Small local helper functions are acceptable only if they remove repetitive
boilerplate without hiding the main logic. Notebook 01's
`worst_case_bond_dims(...)` is the current model for an acceptable helper.

### 7. Parameter presentation

Important parameters should be explicit, named, and placed close to the code
that uses them.

Examples:

- `R`
- `value_type`
- `tolerance`
- `maxbonddim`
- `maxiter`
- interval bounds
- chosen sample index

If a parameter is visible but not yet active in the example, explain that.
Notebook 01 already does this for `maxbonddim = 32`, which is shown
conceptually but does not bind the example.

### 8. API explanation rule

If an API call contains something that looks strange to a first-time reader,
explain it immediately.

Notebook 01 sets the standard with:

- explicit explanation of the `Float64` value-type argument,
- explanation of why multiple representations are introduced,
- explanation of what each conversion makes possible.

### 9. Representation conversion rule

When converting between objects, always explain:

- what the current object contains,
- why the next representation is introduced,
- what concrete operation becomes possible after the conversion.

Use concrete language. Do not hide behind vague phrases like
"tensor-network API" unless the notebook immediately names the concrete
functions being used.

### 10. Variable naming

Variable names should be readable enough to teach from.

Preferred examples:

- `target_function`
- `bond_dims`
- `sample_coordinate`
- `sample_exact_value`
- `experiment_max_abs_error`

When comparing several values, encode the comparison into the names. Notebook
01 models this well with:

- `sample_qtt_value`
- `sample_indexed_tt_value`
- `sample_exact_value`

### 11. Printed output

Use compact, sentence-like `println(...)` output for the main teaching flow.

Good print targets:

- grid extent,
- parameter values,
- bond dimensions,
- maximum absolute errors,
- one representative sample point.

Avoid:

- `@show` in the main walkthrough,
- raw large arrays,
- full tensors,
- large object dumps,
- debug-heavy output.

### 12. Plotting standard

All notebooks should use `CairoMakie`.

Notebook 01 defines the current visual grammar.

#### Standard two-panel figure

For a common "example plus structure" layout, use:

```julia
Figure(size=(1000, 380))
```

Preferred panel roles:

- left panel: physical-space object or approximation,
- right panel: structural quantity such as bond dimensions.

#### One-dimensional function plots

Use the following defaults where they fit:

- exact/reference curve:
  - color `:black`
  - `linewidth=2`
- QTT samples:
  - color `:deepskyblue4`
  - `markersize=5` or `6`, depending on density

Typical labels:

- `xlabel="x"`
- `ylabel="value"`

Typical legend position:

- `position=:lt`

Titles should be short and literal, for example:

- `"cosh(x) on a quantics grid"`
- `"Second experiment on [-1, 2]"`

#### Bond-dimension plots

Use the following defaults where they fit:

- observed bond dimensions:
  - color `:goldenrod2`
  - `linewidth=2`
- worst-case envelope:
  - color `:gray60`
  - `linewidth=2`
  - `linestyle=Linestyle([0, 10, 15])`

Typical labels:

- `xlabel="bond link"`
- `ylabel="bond dimension"`

Typical scaling:

- `yscale=log2`

Typical legend position:

- `position=:rb`

The log base 2 scaling is part of the teaching design, not a cosmetic extra.

#### Color semantics

Keep color meaning stable across notebooks:

- black = exact/reference,
- blue = QTT values or approximation samples,
- golden/orange = observed structural quantity,
- gray = comparison envelope or bound.

#### Labeling rules

Every plot should have:

- x-label,
- y-label,
- title if helpful,
- legend whenever several visual elements appear.

Labels should be literal and readable, not decorative.

### 13. Experiment sections

Each notebook should include at least one small experiment or controlled
variation.

That variation should:

- reuse most of the main workflow,
- change one main ingredient,
- tell the student exactly what they are invited to modify.

Notebook 01 models this with:

- same interpolation workflow,
- shifted interval,
- simple second function,
- explicit invitation to change `experiment_function`, interval bounds, or `R`.

### 14. Closing sections

Every notebook should end with:

- `## What to notice`
- `## API recap`

`What to notice` summarizes conceptual takeaways.

`API recap` summarizes the concrete package entry points just used.

These sections should be short, scannable, and consistent across the series.

### 15. Things to avoid

Do not:

- mix major headings and long explanatory text in the same Markdown cell,
- introduce unexplained jargon such as "indexed chain",
- hide core logic in helpers,
- invent notebook-local replacements for missing library functionality,
- change plot colors and layout arbitrarily between notebooks,
- print raw large objects,
- mention feature gaps inside the learner-facing notebook text.

---

## Task 0: Refresh Against Final Rust Tutorials

**Files:**
- Review: `../rust-Tensor4all/docs/tutorials/*.md`
- Review: `../rust-Tensor4all/docs/index.md`
- Review: `../rust-Tensor4all/src/bin/*.rs`
- Review: `docs/tutorial-learning-path.md`
- Modify if needed: `docs/superpowers/plans/2026-04-23-tensor4all-jl-tutorial-notebooks.md`

- [ ] **Step 1: Confirm the Rust tutorial cleanup is ready to port**

Ask the project owner whether the Rust tutorials are stable enough to use as
the source material for the Julia notebook prototype.

Expected:

- Proceed only after the answer is yes.

- [ ] **Step 2: List the current Rust tutorial files**

Run:

```bash
rg --files ../rust-Tensor4all/docs/tutorials ../rust-Tensor4all/src/bin
```

Expected:

- The output includes the Rust tutorial Markdown files and matching demo
  binaries.

- [ ] **Step 3: Re-read the Rust tutorial index**

Run:

```bash
sed -n '1,220p' ../rust-Tensor4all/docs/index.md
```

Expected:

- The reading order is visible.
- Any changed tutorial names or order are noted.

- [ ] **Step 4: Compare Rust tutorial names against the Julia learning path**

Check this mapping:

```text
tt_basics                 -> not ported as a Julia tutorial
qtt_function              -> 01_first_qtt_function_and_grid.ipynb
qtt_interval              -> 01_first_qtt_function_and_grid.ipynb
qtt_r_sweep               -> 02_accuracy_bonddims_and_sweeps.ipynb
qtt_multivariate          -> 03_multivariate_qtts_and_layouts.ipynb
qtt_elementwise_product   -> 04_operations_on_qtts.ipynb
qtt_integral              -> 04_operations_on_qtts.ipynb
qtt_fourier               -> 05_fourier_transforms.ipynb
qtt_partial_fourier2d     -> 05_fourier_transforms.ipynb
qtt_affine                -> 06_affine_transformations.ipynb
```

Expected:

- If the Rust tutorial set changed, update `docs/tutorial-learning-path.md` and
  this plan before continuing.
- Keep the explicit decision that `tt_basics` is excluded from the Julia
  notebook set.

- [ ] **Step 5: Re-check Notebook 01 source material**

Run:

```bash
sed -n '1,260p' ../rust-Tensor4all/docs/tutorials/qtt_function_tutorial.md
sed -n '1,220p' ../rust-Tensor4all/docs/tutorials/qtt_interval_tutorial.md
```

Expected:

- The `cosh(x)` example and grid/interval explanation are still appropriate for
  Notebook 01.
- If the Rust cleanup changed the first example, pause and ask whether the
  Julia prototype should still start with `cosh(x)`.
- Ignore `tt_basics_tutorial.md` when deciding the Julia prototype scope.

---

## Task 1: Create The Julia Environment

**Files:**
- Create: `Project.toml`
- Create: `Manifest.toml`

- [ ] **Step 1: Add notebook dependencies using Julia Pkg**

Run:

```bash
julia --project=. -e 'using Pkg; Pkg.add(url="https://github.com/tensor4all/Tensor4all.jl.git"); Pkg.add(["IJulia", "CairoMakie"]); Pkg.instantiate(); Pkg.build("Tensor4all"); Pkg.precompile()'
```

Expected:

- `Project.toml` exists.
- `Manifest.toml` exists.
- `Tensor4all`, `IJulia`, and `CairoMakie` are in the environment.
- `Pkg.build("Tensor4all")` completes, or fails with a clear backend build error that should be recorded in `docs/library-gap-log.md`.
- `Pkg.precompile()` completes before users open the notebook in VS Code.

- [ ] **Step 2: Verify the environment imports**

Run:

```bash
julia --project=. -e 'using Tensor4all; using CairoMakie; using IJulia; println("tutorial environment ok")'
```

Expected:

```text
tutorial environment ok
```

- [ ] **Step 3: If working against a local Tensor4all.jl checkout, override only locally**

Run this only for local contributor work:

```bash
julia --project=. -e 'using Pkg; Pkg.develop(path="../../code/Tensor4all/Tensor4all.jl"); Pkg.instantiate(); Pkg.build("Tensor4all"); Pkg.precompile()'
```

Expected:

- The local checkout is used in this working copy.
- The notebooks do not contain the local path.
- Learner setup still works from GitHub through `Project.toml` and `Manifest.toml`.

---

## Task 2: Write The README Setup Instructions

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace `README.md` with a learner-facing introduction**

Use this content:

```markdown
# Tensor4all.jl Tutorials

Jupyter notebooks for learning Quantics Tensor Trains (QTTs) with
`Tensor4all.jl`.

The tutorials are written for Master students and early PhD students who are
learning QTTs for the first time. The focus is interactive learning: open a
notebook, run the cells, change parameters, and inspect the plots.

## Setup

Install Julia 1.9 or later first.

Then clone this repository and instantiate the Julia environment:

```bash
git clone https://github.com/sdirnboeck/Tensor4all.jl-TUTORIALS.git
cd Tensor4all.jl-TUTORIALS
julia --project=. -e 'using Pkg; Pkg.instantiate(); Pkg.build("Tensor4all"); Pkg.precompile()'
```

The notebooks use `IJulia`, which provides the Julia kernel for Jupyter
notebooks, and `CairoMakie` for plotting.

The explicit `Pkg.precompile()` step avoids triggering package precompilation
from inside a VS Code notebook kernel.

## Opening The Notebooks

You can open the notebooks in Jupyter, VS Code, or another IDE that supports
Julia notebooks.

Start with:

```text
01_first_qtt_function_and_grid.ipynb
```

## Learning Path

```text
01_first_qtt_function_and_grid.ipynb
02_accuracy_bonddims_and_sweeps.ipynb
03_multivariate_qtts_and_layouts.ipynb
04_operations_on_qtts.ipynb
05_fourier_transforms.ipynb
06_affine_transformations.ipynb
```

The first notebook introduces a scalar function, a quantics grid, and the basic
QTT workflow. Later notebooks build toward accuracy experiments, multivariate
functions, QTT operations, Fourier transforms, and affine transformations.

## Local Contributor Setup

If you are developing these tutorials together with a local checkout of
`Tensor4all.jl`, you can override the package dependency locally:

```bash
julia --project=. -e 'using Pkg; Pkg.develop(path="../../code/Tensor4all/Tensor4all.jl"); Pkg.instantiate(); Pkg.build("Tensor4all"); Pkg.precompile()'
```

Do not put local machine paths inside notebook cells.
```

- [ ] **Step 2: Verify README renders as plain Markdown**

Run:

```bash
sed -n '1,220p' README.md
```

Expected:

- The setup section is visible.
- The notebook order is visible.
- The local contributor setup is separate from learner setup.

---

## Task 3: Create The Local Library Gap Log

**Files:**
- Create: `docs/library-gap-log.md`

- [ ] **Step 1: Add the gap-log file**

Use this content:

```markdown
# Tensor4all.jl Tutorial Gap Log

This file records missing or unclear `Tensor4all.jl` functionality encountered
while writing the tutorial notebooks.

The teaching notebooks should not contain development-backlog notes. If a
missing feature blocks a notebook section, record it here first. GitHub issues
can be created later after human review.

Each entry should be understandable without reading the notebooks. A short note
about where the issue surfaced in the tutorial work is useful, but the main
description should stand alone as a package-level note.

## Entries

No gaps recorded yet.
```

- [ ] **Step 2: Verify the file exists**

Run:

```bash
sed -n '1,120p' docs/library-gap-log.md
```

Expected:

- The file explains that gaps are tracked outside notebooks.
- The file explains that entries should be understandable without notebook
  context.
- It contains the line `No gaps recorded yet.`

---

## Task 4: Generate Prototype Notebook 01 Only

**Files:**
- Create: `01_first_qtt_function_and_grid.ipynb`

**Important:** This task is now primarily a historical record of how Notebook
01 was first scaffolded. The notebook already exists and has since been
reviewed and refined. If any instruction in Task 4 conflicts with the current
contents of `01_first_qtt_function_and_grid.ipynb` or with the "Reference
Notebook Style Specification" above, the current notebook file and the style
specification are the source of truth.

Do not regenerate Notebook 01 from the older text in this task unless a human
explicitly asks for that.

- [ ] **Step 1: Create a new Jupyter notebook**

Create:

```text
01_first_qtt_function_and_grid.ipynb
```

Use the Julia kernel from the repository environment.

The notebook title cell must be:

```markdown
# First QTT Function And Grid

In this notebook we build a first Quantics Tensor Train (QTT) approximation
with `Tensor4all.jl`. We start with `cosh(x)` because it has a simple structure:
on a quantics grid its QTT representation has very small bond dimension, and
that can be understood by hand.
```

- [ ] **Step 2: Add learning goals**

Add a Markdown cell:

```markdown
## Learning Goals

After this notebook you should be able to:

- choose a bit depth `R` and understand the grid size `2^R`,
- map discrete grid indices to physical coordinates,
- build a QTT approximation of a scalar function,
- evaluate the QTT on grid points,
- compare QTT values against the original function,
- inspect bond dimensions,
- rerun the workflow for a different function.
```

- [ ] **Step 3: Add imports**

Add a Julia code cell:

```julia
using Tensor4all
using CairoMakie
```

Expected output:

- No printed output is needed.
- The cell should run without error.

- [ ] **Step 4: Add grid parameters**

Add a Markdown cell:

```markdown
## A First Quantics Grid

The bit depth `R` determines the number of grid points. A one-dimensional
quantics grid with bit depth `R` has `2^R` points.

We start on the interval `[0, 1]`.
```

Add a Julia code cell:

```julia
R = 6
npoints = 2^R
xmin = 0.0
xmax = 1.0

xgrid = range(xmin, xmax; length=npoints)

(R=R, npoints=npoints, interval=(xmin, xmax))
```

Expected output shape:

```text
(R = 6, npoints = 64, interval = (0.0, 1.0))
```

- [ ] **Step 5: Add the first target function**

Add a Markdown cell:

```markdown
## The Target Function

We start with `cosh(x)`. This is a useful first example because it is smooth and
has a simple low-rank structure in QTT form.
```

Add a Julia code cell:

```julia
target_function(x) = cosh(x)

reference_values = target_function.(xgrid)

(first_value=first(reference_values), last_value=last(reference_values))
```

Expected output:

- A compact named tuple with the first and last values.
- No full vector printout.

- [ ] **Step 6: Add visible bit-grid helper functions**

Add a Markdown cell:

```markdown
## From Bits To Coordinates

The QTT is built over binary site indices. Each site value is either `1` or
`2`, representing a bit value `0` or `1`. The following small functions keep
that mapping visible.
```

Add a Julia code cell:

```julia
function index_to_bit_values(i, R)
    index0 = i - 1
    return [((index0 >> (k - 1)) & 1) + 1 for k in 1:R]
end

function bit_values_to_grid_index(values)
    index0 = 0
    for (k, value) in enumerate(values)
        bit = Int(value) - 1
        index0 += bit * 2^(k - 1)
    end
    return index0 + 1
end

function bit_values_to_x(values; xmin, xmax, R)
    grid_index = bit_values_to_grid_index(values)
    return xmin + (grid_index - 1) * (xmax - xmin) / (2^R - 1)
end

(index_1_bits=index_to_bit_values(1, R), index_last_bits=index_to_bit_values(npoints, R))
```

Expected:

- A compact named tuple showing the first and last bit encodings.
- The helper functions are visible in the notebook.

- [ ] **Step 7: Add the QTT construction cell**

Use this public Tensor4all.jl API chain:

```text
Tensor4all.TensorCI.crossinterpolate2
Tensor4all.SimpleTT.TensorTrain
Tensor4all.TensorNetworks.TensorTrain
```

Add a Julia code cell:

```julia
const TN = Tensor4all.TensorNetworks

function qtt_for_function(f; R, xmin, xmax, tolerance, maxbonddim)
    local_dimensions = fill(2, R)
    sample_on_bits = bit_values -> f(bit_values_to_x(bit_values; xmin, xmax, R))

    tci = Tensor4all.TensorCI.crossinterpolate2(
        Float64,
        sample_on_bits,
        local_dimensions;
        tolerance=tolerance,
        maxbonddim=maxbonddim,
    )

    simple_tt = Tensor4all.SimpleTT.TensorTrain(tci)
    sites = [Index(2; tags=["bit", "bit=$k"]) for k in 1:R]
    qtt = TN.TensorTrain(simple_tt, sites)

    return (; tci, simple_tt, qtt, sites)
end

tolerance = 1e-12
maxbonddim = 20

cosh_qtt = qtt_for_function(
    target_function;
    R=R,
    xmin=xmin,
    xmax=xmax,
    tolerance=tolerance,
    maxbonddim=maxbonddim,
)

(sites=length(cosh_qtt.sites), tolerance=tolerance, maxbonddim=maxbonddim)
```

Expected:

- The cell runs without defining any hidden QTT implementation.
- The named tuple reports the number of sites and interpolation parameters.

If this API chain does not work with the current `Tensor4all.jl`, do not invent
a notebook-local QTT constructor. Instead:

1. record the missing API in `docs/library-gap-log.md`,
2. stop notebook implementation,
3. report that Notebook 01 is blocked by missing or unclear public API.

- [ ] **Step 8: Add QTT evaluation**

Add a Julia code cell:

```julia
function evaluate_on_grid(qtt, sites; R)
    point_matrix = reduce(hcat, [index_to_bit_values(i, R) for i in 1:(2^R)])
    return real.(TN.evaluate(qtt, sites, point_matrix))
end

qtt_values = evaluate_on_grid(cosh_qtt.qtt, cosh_qtt.sites; R=R)

maximum_abs_error = maximum(abs.(qtt_values .- reference_values))

(maximum_abs_error=maximum_abs_error,)
```

Expected:

- The output is a compact named tuple.
- Evaluation uses `Tensor4all.TensorNetworks.evaluate`.

- [ ] **Step 9: Add a labeled comparison plot**

Add a Julia code cell:

```julia
fig = Figure(size=(720, 420))
ax = Axis(
    fig[1, 1],
    xlabel="x",
    ylabel="f(x)",
    title="cosh(x): analytic function and QTT samples",
)

lines!(ax, xgrid, reference_values; label="analytic cosh(x)", linewidth=2)
scatter!(ax, xgrid, qtt_values; label="QTT samples", markersize=7)
axislegend(ax; position=:lt)

fig
```

Expected:

- A rendered CairoMakie figure.
- Labeled x-axis and y-axis.
- Legend with analytic and QTT values.

- [ ] **Step 10: Add bond-dimension inspection**

Add a Markdown cell:

```markdown
## Bond Dimensions

Bond dimensions describe the internal size of the tensor train. For this first
example, they should stay small. That is the compression benefit we want to
notice.
```

Add a code cell:

```julia
bond_dimensions = TN.linkdims(cosh_qtt.qtt)

(bond_dimensions=bond_dimensions, maximum_bond_dimension=maximum(bond_dimensions))
```

Expected:

- The output is compact.
- Bond dimensions are obtained through `Tensor4all.TensorNetworks.linkdims`.

- [ ] **Step 11: Add a bond-dimension plot**

Add a Julia code cell:

```julia
fig = Figure(size=(720, 360))
ax = Axis(
    fig[1, 1],
    xlabel="bond index",
    ylabel="bond dimension",
    title="Bond dimensions of the cosh(x) QTT",
)

scatterlines!(ax, 1:length(bond_dimensions), bond_dimensions; markersize=8)

fig
```

Expected:

- A rendered CairoMakie figure.
- Axes are labeled.
- The plot shows a compact bond-dimension profile.

- [ ] **Step 12: Add the hand-calculation explanation**

Add a Markdown cell:

```markdown
## Why `cosh(x)` Is A Good First Example

The function `cosh(x)` can be written as

    cosh(x) = (exp(x) + exp(-x)) / 2

On a quantics grid, the binary representation of the grid index decomposes the
coordinate into a sum of bit contributions. Exponentials of sums factor into
products of exponentials. This is why exponential functions, and therefore
`cosh(x)` as a sum of two exponentials, admit a very compact QTT
representation.

The important observation for now is not the full derivation. It is that some
smooth structured functions can have very small QTT bond dimensions.
```

- [ ] **Step 13: Add a try-your-own-function experiment**

Add a Markdown cell:

```markdown
## Experiment: Try Another Function

Change `experiment_function` and rerun the next cells. Watch how the error and
bond dimensions change.
```

Add code cells that repeat the same workflow for a second function:

```julia
experiment_function(x) = exp(-40 * (x - 0.35)^2) + 0.2 * sin(12π * x)

experiment_values = experiment_function.(xgrid)

(minimum_value=minimum(experiment_values), maximum_value=maximum(experiment_values))
```

Then add code cells that reuse the visible workflow:

```julia
experiment_qtt = qtt_for_function(
    experiment_function;
    R=R,
    xmin=xmin,
    xmax=xmax,
    tolerance=tolerance,
    maxbonddim=maxbonddim,
)

experiment_qtt_values = evaluate_on_grid(experiment_qtt.qtt, experiment_qtt.sites; R=R)
experiment_error = maximum(abs.(experiment_qtt_values .- experiment_values))
experiment_bond_dimensions = TN.linkdims(experiment_qtt.qtt)

(maximum_abs_error=experiment_error, maximum_bond_dimension=maximum(experiment_bond_dimensions))
```

Add a comparison plot with this title:

```text
Experiment function: analytic values and QTT samples
```

- [ ] **Step 14: Add API recap**

Add a Markdown cell:

```markdown
## API Recap

In this notebook we used:

- `Tensor4all.TensorCI.crossinterpolate2` to construct a tensor cross interpolation,
- `Tensor4all.SimpleTT.TensorTrain` to convert interpolation output to raw TT form,
- `Tensor4all.TensorNetworks.TensorTrain` to attach Tensor4all indices,
- `Tensor4all.TensorNetworks.evaluate` to evaluate QTT values,
- `Tensor4all.TensorNetworks.linkdims` to inspect bond dimensions,
- CairoMakie for labeled plots.

The main workflow was:

```text
function -> grid -> QTT -> evaluated values -> error and plots
```
```

- [ ] **Step 15: Run the full notebook**

Run all cells from top to bottom.

Expected:

- No cell errors.
- No full tensor or large array output.
- At least two labeled function comparison plots.
- At least one labeled bond-dimension plot.
- Compact scalar or tuple outputs only.

- [ ] **Step 16: Stop for human review**

Do not generate notebooks `02` through `06` yet.

Report:

- whether Notebook 01 runs,
- which Tensor4all.jl APIs were used,
- whether any gaps were recorded,
- whether the teaching flow feels too long, too short, or appropriate.

---

## Task 5: Review Checkpoint After Notebook 01

**Files:**
- Review: `01_first_qtt_function_and_grid.ipynb`
- Review: `docs/library-gap-log.md`
- Optionally modify: `docs/tutorial-learning-path.md`

- [ ] **Step 1: Review Notebook 01 for teaching quality**

Check:

- Does the notebook start gently?
- Is `R` explained before it is used heavily?
- Is the code visible and modifiable?
- Are plots labeled clearly?
- Is the `cosh(x)` bond-dimension motivation understandable?
- Is the second function useful as an experiment?

- [ ] **Step 2: Decide whether to keep the combined first notebook**

Choose one:

```text
Option A: Keep 01 as combined function-and-grid notebook.
Option B: Split 01 into function notebook and grid notebook.
Option C: Merge 01 and 02 later into one longer introductory notebook.
```

- [ ] **Step 3: Apply review changes before continuing**

Make only the changes requested during review. Do not start Notebook 02 until
Notebook 01 style is approved.

---

## Task 6: Generate Notebook 02 After Review Approval

**Files:**
- Create: `02_accuracy_bonddims_and_sweeps.ipynb`

- [ ] **Step 1: Create notebook title and goals**

Notebook title:

```markdown
# Accuracy, Bond Dimensions, And Sweeps
```

Learning goals:

```markdown
- understand approximation error,
- vary bit depth `R`,
- vary interpolation tolerance,
- inspect bond-dimension growth,
- compare multiple target functions,
- interpret error and compression together.
```

- [ ] **Step 2: Build from the approved Notebook 01 style**

Use the same:

- imports,
- CairoMakie style,
- compact outputs,
- visible parameter cells,
- public Tensor4all.jl APIs.

- [ ] **Step 3: Include these sections**

Required sections:

```text
1. One function, several tolerances
2. One function, several bit depths R
3. Error versus bond dimension
4. Runtime observations if easy to measure
5. What to notice
6. API recap
```

- [ ] **Step 4: Run all cells**

Expected:

- No errors.
- Plots show labeled error and bond-dimension comparisons.
- No large raw tensor output.

---

## Task 7: Generate Notebook 03 After Review Approval

**Files:**
- Create: `03_multivariate_qtts_and_layouts.ipynb`

- [ ] **Step 1: Create notebook around one clear 2D example**

Use one smooth two-dimensional function and compare reference values against
QTT values. Keep the first 2D example simple enough to plot clearly.

- [ ] **Step 2: Include layout discussion only where supported**

If interleaved, grouped, or fused layouts are supported by public APIs, compare
them. If not, record the missing layout functionality in `docs/library-gap-log.md`
and teach only the supported layout.

- [ ] **Step 3: Run all cells**

Expected:

- At least one labeled 2D reference plot.
- At least one labeled 2D QTT-value plot or error plot.
- Compact bond-dimension summary.

---

## Task 8: Generate Notebook 04 After Review Approval

**Files:**
- Create: `04_operations_on_qtts.ipynb`

- [ ] **Step 1: Teach elementwise multiplication first**

Use two univariate functions on the same grid:

```julia
f(x) = exp(-20 * (x - 0.35)^2)
g(x) = cos(6π * x)
```

Teach:

- build QTT for `f`,
- build QTT for `g`,
- compute QTT representation of `h(x) = f(x) * g(x)` using public API,
- compare with analytic product,
- inspect rank growth.

- [ ] **Step 2: Teach integration second**

Use a single univariate function on an interval. Compute an integral using
public Tensor4all.jl API. Compare against an analytic value or a high-resolution
numerical reference.

If integration is not supported by public API, record the gap and stop this
section.

- [ ] **Step 3: Run all cells**

Expected:

- Labeled product comparison plot.
- Bond-dimension comparison before and after multiplication.
- Integral result shown as compact scalar output.

---

## Task 9: Generate Notebook 05 After Review Approval

**Files:**
- Create: `05_fourier_transforms.ipynb`

- [ ] **Step 1: Teach 1D Fourier transform**

Use one simple signal, apply the public Tensor4all.jl Fourier transform API,
and compare against a dense or analytic reference where feasible.

- [ ] **Step 2: Teach 2D partial Fourier transform**

Use one 2D function and transform only one coordinate. Keep all Fourier-related
material in this notebook.

- [ ] **Step 3: Run all cells**

Expected:

- Labeled before/after plots.
- Bond-dimension summaries before and after transformation.
- Clear explanation of which coordinate is transformed.

---

## Task 10: Generate Notebook 06 After Review Approval

**Files:**
- Create: `06_affine_transformations.ipynb`

- [ ] **Step 1: Teach affine pullback or transformation**

Use the public affine transformation API. Compare transformed QTT values
against reference values.

- [ ] **Step 2: Include boundary behavior only if supported**

If periodic and open boundary behavior are both supported, compare them. If not,
record the gap and teach only the supported behavior.

- [ ] **Step 3: Run all cells**

Expected:

- Labeled reference and transformed plots.
- Compact bond-dimension summary.
- No hidden implementation of affine behavior inside the notebook.

---

## Task 11: Final Repository Pass

**Files:**
- Review: all notebooks
- Review: `README.md`
- Review: `docs/library-gap-log.md`
- Review: `docs/tutorial-learning-path.md`

- [ ] **Step 1: Run every notebook from top to bottom**

Expected:

- All cells pass.
- Outputs are compact.
- Plots are labeled.
- No local machine paths appear in notebook code cells.

- [ ] **Step 2: Search for local paths**

Run:

```bash
rg "../../code|/Users/|Tensor4all-jl|Tensor4all/Tensor4all.jl" .
```

Expected:

- Local paths appear only in contributor setup documentation, not in notebook
  code cells.

- [ ] **Step 3: Search for development notes inside notebooks**

Run:

```bash
rg "missing|gap|TODO|not implemented|workaround" *.ipynb docs README.md
```

Expected:

- Missing-feature notes appear only in `docs/library-gap-log.md` or planning
  documents.
- Teaching notebooks do not contain development-backlog language.

- [ ] **Step 4: Decide output policy**

Check notebook sizes:

```bash
ls -lh *.ipynb
```

If notebooks are reasonably small, keep outputs committed for GitHub browsing.
If a notebook is large because of outputs, clear large outputs while keeping
the notebook runnable.

- [ ] **Step 5: Update README final status**

Ensure README lists every completed notebook and the recommended order.

---

## Self-Review

Spec coverage:

- The implementation pause until Rust cleanup is covered by Task 0 and the
  Execution Rule.
- Jupyter-first format is covered by Tasks 4 through 10.
- Simple learner setup is covered by Tasks 1 and 2.
- Local Tensor4all.jl contributor override is covered by Tasks 1 and 2.
- CairoMakie plotting is required globally and in notebook tasks.
- English prose is required globally.
- Missing features are kept outside notebooks through Task 3 and the global rules.
- First-notebook-only implementation and review is enforced by Tasks 4 and 5.
- The revised six-notebook learning path is reflected in Tasks 4 through 10.

Placeholder scan:

- Notebook 01 uses the concrete public API path currently visible in the local
  Tensor4all.jl docs/tests: `TensorCI.crossinterpolate2`,
  `SimpleTT.TensorTrain`, `TensorNetworks.TensorTrain`,
  `TensorNetworks.evaluate`, and `TensorNetworks.linkdims`.
- Later notebooks are intentionally less detailed until Notebook 01 is reviewed.
  They should inherit the approved style and must stop at the gap log if a
  public API is missing.
