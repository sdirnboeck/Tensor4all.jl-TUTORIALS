name: Notebooks

on:
  push:
    branches: [main, convert-tutorials-to-pluto]
  pull_request:
  workflow_dispatch:

concurrency:
  group: notebooks-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pluto-notebooks:
    name: Pluto notebooks (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v7

      - uses: julia-actions/setup-julia@v3
        with:
          version: '1'

      - uses: julia-actions/cache@v3
        with:
          delete-old-caches: false

      - name: Install Pluto test runner dependency
        shell: bash
        run: |
          julia --color=yes --startup-file=no -e 'import Pkg; Pkg.add("Pluto")'

      - name: Run all Pluto notebooks
        shell: bash
        env:
          JULIA_NUM_THREADS: '2'
          JULIA_PKG_PRECOMPILE_AUTO: '0'
        run: |
          julia --color=yes --startup-file=no .github/scripts/run_pluto_notebooks.jl 0*.jl
